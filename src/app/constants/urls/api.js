const API = 'https://passenger-transport.herokuapp.com/api';

// route
export const GET_ROUTES =  API + '/route';

// transporter
export const GET_TRANSPORTERS = API + '/transporter';

// driver
export const GET_DRIVERS = API + '/employee/driver';

// assistant
export const ASSISTANT_URL = API + '/employee/assistant';

// client
export const CLIENT_URL = API + '/client';

// schedule
export const TRANSPORT_SCHEDULE_URL = API + '/transport-schedule';
export const WORK_SCHEDULE_URL = API + '/work-schedule';

// ticket
export const TICKET_URL = API + '/ticket';